name: Clinician Management Flow

on:
  push:
    branches: [main, Sainath_Gaikwad]
  pull_request:
    branches: [main, Sainath_Gaikwad]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: List files for debug
        run: ls -R
        working-directory: .

      - name: Install dependencies
        run: cd ecare-api-testing-framework-1 && npm install
      - name: Install Playwright Browsers
        run: cd ecare-api-testing-framework-1 && npx playwright install
      - name: Run Playwright tests
        run: cd ecare-api-testing-framework-1 && npx playwright test tests/clinician-management.spec.js
      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: ecare-api-testing-framework-1/playwright-report/